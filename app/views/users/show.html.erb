<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
<h3>My Totall</h3>
<p><%= link_to number_to_currency(@totall), user_accounts_path(params[:id]) %></p>
<%= link_to 'Edit User', edit_user_path, class: "btn btn-lg btn-primary" %>
