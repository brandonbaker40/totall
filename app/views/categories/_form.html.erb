<%= form_for(@category) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.label :active %>
  <%= f.check_box :active, class: 'form-control' %>

  <%= f.submit %>
<% end %>
